interface:
  display_name: "台股平台运营"
  short_description: "台股 Threads 运营规则维护与 persona 治理 skill"
  default_prompt: "Use $platform-operations to maintain and audit a Taiwan stock Threads operations skill, especially persona governance, compliance review, validation, and changelog updates. Only use the optional content templates when the user explicitly asks for content-production testing."

policy:
  allow_implicit_invocation: true
