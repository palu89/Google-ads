interface:
  display_name: "台股平台运营"
  short_description: "台股 Threads 运营规则维护与 persona 治理 skill"
  default_prompt: "Use $platform-operations to maintain and audit a Taiwan stock Threads operations skill, especially persona governance, compliance review, validation, changelog updates, and the content-operations workflow when the user explicitly asks to start or strengthen Threads content operations. Default to the existing content-operations chain, and do not expand new templates unless a live operational pain point requires it. Only use the optional content templates when the user explicitly asks for content-production testing."

policy:
  allow_implicit_invocation: true
